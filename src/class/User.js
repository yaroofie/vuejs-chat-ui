export default class
{
   constructor( id, username, email, avatar, about )
   {
      this.id = id;
      this.username = username;
      this.email = email;
      this.avatar = avatar;
      this.about = about;
   }
}